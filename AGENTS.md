# AGENTS.md

이 저장소는 사용자가 코드를 직접 실행하고 결과를 관찰하며 딥러닝과 LLM을 학습하기 위한 공간이다.

## 실행 및 검증 원칙

- 에이전트는 학습용 Python 파일을 직접 실행하지 않는다.
- 에이전트는 `uv run python ...` 등의 실습 실행 명령을 자동으로 수행하지 않는다.
- 작성된 학습 코드의 실행과 결과 검증은 사용자가 직접 한다.
- 에이전트는 실행 명령, 예상 결과, 관찰해야 할 핵심 내용을 사용자에게 안내한다.
- 사용자가 실행 결과나 오류 메시지를 전달하면 그 내용을 바탕으로 원인을 설명하거나 코드를 수정한다.
- 사용자가 명시적으로 실행 또는 검증을 요청한 경우에만 에이전트가 실행한다.

## 학습 자료 작성 원칙

- 전체 학습 순서와 프로젝트 구조는 최상위 `README.md`를 따른다.
- 큰 학습 단계는 `01-`, `02-`와 같은 두 자리 번호가 붙은 별도 폴더로 구분한다.
- 폴더 내부의 실습 파일도 `01_`, `02_`와 같은 두 자리 번호를 붙인다.
- 새로운 학습 내용을 추가할 때 기존 실습 파일을 덮어쓰지 않는다.
- 각 실습 파일에는 개념 설명, 단계별 계산 과정과 실행 가능한 예제를 남긴다.
- 새 실습 파일을 추가하면 해당 폴더의 `README.md`에 학습 내용과 실행 명령을 기록한다.
- 최상위 `README.md`의 현재 진행 상황도 함께 갱신한다.
