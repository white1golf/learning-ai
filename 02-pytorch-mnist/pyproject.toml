[project]
name = "pytorch-mnist"
version = "0.1.0"
description = "PyTorch로 MNIST 숫자 분류기를 학습하는 두 번째 실습"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.0",
    "torchvision>=0.15",
]

[dependency-groups]
dev = []
